diff --git a/riscv/processor.h b/riscv/processor.h
index 4f357c6..3e52d83 100644
--- a/riscv/processor.h
+++ b/riscv/processor.h
@@ -161,6 +161,11 @@ struct state_t
 
   bool serialized; // whether timer CSRs are in a well-defined state
 
+  // DiffTest counter control for the instruction currently being executed.
+  bool counter_write_in_instruction;
+  bool mcycle_increment;
+  bool minstret_increment;
+
   // When true, execute a single instruction and then enter debug mode.  This
   // can only be set by executing dret.
   enum {
@@ -295,6 +300,8 @@ public:
   const char* get_symbol(uint64_t addr);
   
   bool is_waiting_for_interrupt() { return in_wfi; };
+  // NEMU DiffTest: NEMU uses the architecturally permitted WFI-as-NOP behavior.
+  void resume_from_wfi() { in_wfi = false; }
 
   void take_trap_public(trap_t& t, reg_t epc) { take_trap(t, epc); }
 
