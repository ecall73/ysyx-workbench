//| mill-version: 1.1.0

package build

import mill._
import mill.scalalib.scalafmt.ScalafmtModule
import mill.scalalib._

object `package` extends ScalaModule with ScalafmtModule {
  override def scalaVersion = "2.13.12"

  override def scalacOptions = Seq(
    "-language:reflectiveCalls",
    "-deprecation",
    "-feature",
    "-Xcheckinit"
  )

  override def mvnDeps             = Seq(mvn"org.chipsalliance::chisel:6.7.0")
  override def scalacPluginMvnDeps = Seq(mvn"org.chipsalliance:::chisel-plugin:6.7.0")
}
